{
	"private": true,
	"type": "module",
	"scripts": {
		"minify:svg": "svgo --config=.svgo.config.cjs --quiet --recursive --folder img/"
	},
	"dependencies": {
		"fs": "^0.0.1-security",
		"glob": "^13.0.4",
		"purgecss": "^8.0.0",
		"svgo": "4.0.0"
	}
}
